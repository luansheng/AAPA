% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_io.R
\name{read_genotype}
\alias{read_genotype}
\title{Read genotype dosage matrix}
\usage{
read_genotype(file, sep = ",", header = TRUE)
}
\arguments{
\item{file}{Path to a CSV/TSV file. First column is individual ID,
remaining columns are SNP dosage values (0, 1, 2, or NA).}

\item{sep}{Field separator (default: comma).}

\item{header}{Logical; does the file contain a header row? Default TRUE.}
}
\value{
A numeric matrix with rownames = individual IDs,
colnames = marker names.
}
\description{
Read a genotype matrix in dosage format (0/1/2, NA for missing).
Rows are individuals, columns are SNP markers.
}
\examples{
# Create a temporary genotype file
tmp <- tempfile(fileext = ".csv")
geno_data <- data.frame(
  id = c("IND1", "IND2"),
  SNP1 = c(0, 1),
  SNP2 = c(2, 1)
)
write.csv(geno_data, tmp, row.names = FALSE)
geno <- read_genotype(tmp)
unlink(tmp)
}
\seealso{
Other data-io: 
\code{\link{read_anchors}()},
\code{\link{read_parents}()},
\code{\link{simulate_aapa_data}()}
}
\concept{data-io}

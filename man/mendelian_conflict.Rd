% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring.R
\name{mendelian_conflict}
\alias{mendelian_conflict}
\title{Compute Mendelian conflict rate}
\usage{
mendelian_conflict(genotype, parents, test_ids = NULL)
}
\arguments{
\item{genotype}{Numeric matrix (individuals x SNPs), dosage 0/1/2/NA.}

\item{parents}{An \code{aapa_parents} object (from \code{read_parents()} or
a named list of families with \code{sire_geno} and \code{dam_geno}).}

\item{test_ids}{Character vector of individual IDs to test. If NULL,
all non-parent individuals in genotype are used.}
}
\value{
A numeric matrix of dimension (n_test x n_families) with
Mendelian conflict rates in \code{[0, 1]}.
}
\description{
For each test individual and each candidate family, calculate the
proportion of loci where the individual's genotype is incompatible
with the expected offspring genotypes given the parental cross.
}
\examples{
sim <- simulate_aapa_data(n_families = 3, n_snps = 100)
tmp <- tempfile(fileext = ".csv")
write.csv(sim$parents, tmp, row.names = FALSE)
parents <- read_parents(tmp, genotype_matrix = sim$genotype)
cm <- mendelian_conflict(sim$genotype, parents)
unlink(tmp)
}
\seealso{
Other scoring: 
\code{\link{anchor_kinship}()},
\code{\link{composite_score}()}
}
\concept{scoring}

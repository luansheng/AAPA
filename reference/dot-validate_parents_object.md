# Validate parents object contract

Check that a parent list contains the required fields, stable family
names, and marker-named parent genotype vectors.

## Usage

``` r
.validate_parents_object(parents)
```

## Arguments

- parents:

  An `aapa_parents` object or compatible named list.

## Value

The input parents object, invisibly.

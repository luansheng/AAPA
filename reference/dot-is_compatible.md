# Check compatibility of individual genotypes with allowed set

Check compatibility of individual genotypes with allowed set

## Usage

``` r
.is_compatible(ind_geno, allowed)
```

## Arguments

- ind_geno:

  Integer vector of individual dosages.

- allowed:

  Logical matrix (n_loci x 3).

## Value

Logical vector; TRUE if compatible.
